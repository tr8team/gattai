cli:
  default:
    command: op
    args:
      {{- with Specs.item-get }}
      - command: item
        args:
          - get {{ .identifier }}
          {{- with .label }}
          - --fields label={{ . }}
          {{- end }}
          {{- with .vault }}
          - --vault {{ . }}
          {{- end }}
      {{- end }}

