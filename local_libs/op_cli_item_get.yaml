version: v1
type: CommandLineInterface
params:
  identifier:
    desc: The one password identifier
    requied: true
    type: string
  label:
    desc:
    requied: true
    type: string
  vault:
    desc:
    requied: true
    type: string
return: "stdout"
spec:
  # what is this? 
  #environment:
  #  kind: docker
  #  repo:
  #test:
  #  - cmd:
  cmds:
    - cmd:
      {{- with .Args }}
      {{- with .identifier }}
      - op item get {{ . }}
      {{- end }}
      {{- with .label }}
      - --fields label={{ . }}
      {{- end }}
      {{- with .vault }}
      - --vault {{ . }}
      {{- end }}
      {{- end }}
    - cmd:
      {{- with .Args }}
      {{- with .identifier }}
      - op item get {{ . }}
      {{- end }}
      {{- with .label }}
      - --fields label={{ . }}
      {{- end }}
      {{- with .vault }}
      - --vault {{ . }}
      {{- end }}
      {{- end }}
